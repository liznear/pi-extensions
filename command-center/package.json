{
	"name": "command-center-manual",
	"version": "0.1.0",
	"description": "Desktop mission control for coordinated agent work.",
	"author": "Command Center",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@biomejs/biome": "2.5.6",
		"@types/bun": "latest"
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"check": "biome check .",
		"check:fix": "biome check . --fix",
		"verify": "bun run check && bun run typecheck && bun test"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@earendil-works/pi-coding-agent": "^0.83.0"
	}
}
