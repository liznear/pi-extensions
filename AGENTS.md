# AGENTS.md

This repo provides a list of extensions for Pi Coding Agent.

## Processes

- Use `bun run verify` to make sure there is no lint / format / build errors.

## Core Principles

- Make changes in this repo. DO NOT change anything in ~/.pi/agents.
- Load this repo using `pi -ne -e <this repo>`.
